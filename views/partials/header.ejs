<!-- Navigation -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark static-top">
        <!-- Navbar brand -->
        <a class="navbar-brand" href="/">Look4Makan</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav mr-auto">
                <!--<li class="nav-item active">-->
                    <!--<a class="nav-link" href="/">Home-->
                        <!--<span class="sr-only">(current)</span>-->
                    <!--</a>-->
                <!--</li>-->
                <!--<li class="nav-item">-->
                    <!--<a class="nav-link" href="/restaurants">Restaurants</a>-->
                <!--</li>-->
                <!--&lt;!&ndash;<li class="nav-item">&ndash;&gt;-->
                    <!--&lt;!&ndash;<a class="nav-link" href="#">Make Reservations</a>&ndash;&gt;-->
                <!--&lt;!&ndash;</li>&ndash;&gt;-->
            </ul>


            <ul class="navbar-nav">

                <li class="nav-item active" style="margin-right:100px">
                    <a class="btn btn-primary" href="rateReservations">Rate Restaurants</a>
                </li>

                <% if (typeof user !== "undefined") { %>
                <li class="nav-item active">
                    <% if (user.isAdmin) { %>
                    <a class="nav-link" href="/edit"><%= user.username %>
                        <span class="sr-only">(current)</span>
                    </a>
                    <% } else { %>
                    <a class="nav-link" href="#"><%= user.username %>
                        <span class="sr-only">(current)</span>
                    </a>
                    <% } %>
                </li>
                <% } %>
                <li class="nav-item active">
                <% if (typeof user !== "undefined" && user.isAdmin) { %>
                <li class="nav-item">
                    <form class="d-none d-md-inline-block form-inline ml-auto mr-0 mr-md-3 my-2 my-md-0" action="/search">
                        <input class="form-control " type="text" placeholder="Type Restaurant name..." aria-label="Search" name="restaurant">
                    </form>
                </li>
                <% } %>
                <% if (typeof loginPage === 'undefined' || !loginPage) { %>
                <li class="nav-item">
                    <% if(auth) { %>
                    <a class="btn btn-primary" href="/logout">Log Out</a>
                    <% } else { %>
                    <a class="btn btn-primary" href="/signin">Sign In</a>
                    <% } %>
                </li>
                <% } %>
            </ul>
        </div>
</nav>

